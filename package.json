{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-test-production": "newman run --disable-unicode tests/Jsonplaceholder.com.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "Josue Becerril",
  "dependencies": {
    "newman": "^5.2.2"
  }
}
